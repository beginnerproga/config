server:
  port: 8761
eureka:
  hostname: ${EUREKA_HOSTNAME:localhost}
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone:
        http://eureka.hostname:${server.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 5

